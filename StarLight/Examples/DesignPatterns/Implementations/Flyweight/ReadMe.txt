There is currently no Compose* implementation of the Flyweight design pattern. Flyweights and the FlyweightFactory are implemented modular in classes. In the default pattern implementation there are no crosscutting concerns.


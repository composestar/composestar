<?xml version="1.0" encoding="UTF-8"?>
<project name="ComposeStar/StarLight/BuildInWeaveStrategies" default="build">

	<condition property="build.core" value="${basedir}/../../../Core">
		<and>
			<not><isset property="build.core"/></not>
			<available file="${basedir}/../../../Core/common.xml"/>
		</and>
	</condition>
	<import file="${build.core}/common.xml"/>

	<echo>Processing: ${ant.project.name} ...</echo>
	<description>The Compose* StarLight Weaving Strategies.</description>

	<property name="buildfile" value="StarLight.Weaving.BuildIn.csproj"/>

	<target name="clean" depends="init" description="Clean compile results">
		<msbuild solution="${buildfile}" target="clean" config="${msbuild.config}"/>
	</target>

	<target name="build" depends="init" description="Build the Weaving Strategies">
		<msbuild solution="${buildfile}" target="build" config="${msbuild.config}"/>
	</target>

	<target name="deploy" depends="init" description="Deploy the Weaving Strategies">
		<msbuild solution="${buildfile}" target="deploy" config="${msbuild.config}"/>
	</target>

</project>

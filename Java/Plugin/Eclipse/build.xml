<?xml version="1.0" encoding="UTF-8"?>
<project name="ComposeStar/Java/Plugin/Eclipse" default="build">

	<condition property="build.core" value="../../../Core">
		<and>
			<not>
				<isset property="build.core" />
			</not>
			<available file="../../../Core/common.xml" />
		</and>
	</condition>
	<import file="${build.core}${file.separator}common.xml" />
	
	<echo>Processing: ${ant.project.name} ...</echo>
	
	<description>
	The Compose* Eclipse Java Plugin
	</description>
	
	<target name="init_local" depends="init_eclipse">
		<mkdir dir="${out.build}"/>
		<mkdir dir="${out.dist}"/>
	</target>
	
	<!-- clean -->
	<target name="clean" depends="init">
		<delete dir="${out.build}"/>
		<delete dir="${out.dist}"/>
	</target>
	
	
	<!-- build -->	
	<target name="build" depends="init_local,compile,jar" description="compile the eclipse plugin" />

	<!-- compile -->
	<target name="compile" depends="init_local" >
		<javac srcdir="src" destdir="${out.build}" source="${build.java.version}" target="${build.java.version}" debug="${build.debug}" deprecation="${build.deprecation}">
			<classpath>
				<pathelement location="${build.core}/Plugin/Eclipse/${out.dist}/ComposestarEclipseCore.jar"/>
			</classpath>
			<classpath refid="lib.eclipse" />
		</javac>
	</target>
	
	<!-- create jar -->
	<target name="jar">
		<jar destfile="${out.dist}/ComposestarEclipseJava.jar" manifest="META-INF/MANIFEST.MF">
			<fileset dir="${out.build}" />
		</jar>
	</target>
	
	<!-- install -->	
	<target name="install" depends="build" description="install the eclipse plugin">
		
		<copy todir="${eclipse.pluginsdir}${file.separator}ComposestarJava">
			<fileset dir="${out.dist}" />
		</copy>
		
		<!-- plugin files -->
		<copy todir="${eclipse.pluginsdir}${file.separator}ComposestarJava">
			<fileset dir=".">
				<include name="plugin.*" />
				<include name="META-INF/*" />
			</fileset>
		</copy>
	
	</target>
	
</project>